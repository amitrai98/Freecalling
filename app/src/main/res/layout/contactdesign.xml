<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout  xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/splash"
    >
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
     > 
   <ImageView android:id="@+id/_img"
       android:layout_height="60dp"
       android:layout_width="60dp"
       android:layout_alignParentTop="true"
       android:clickable="true"
       android:src="@drawable/defaultcontactimage"
       android:padding="2dp"
       android:layout_alignParentLeft="true"/> 
    
   <TextView android:id="@+id/_contactname"
       android:layout_height="wrap_content"
       android:layout_width="wrap_content"
       android:gravity="center"
       android:textSize="20dp"
        android:textStyle="bold"
       android:text="name"
       android:layout_marginLeft="70dp"
       android:textColor="@android:color/white"
       android:layout_toRightOf="@+id/_contactimage"/>
   <TextView android:id="@+id/_contactno"
       android:layout_height="wrap_content"
       android:layout_width="wrap_content"
       android:gravity="right"
       android:textSize="20dp"
       android:textColor="@android:color/white"
       android:textStyle="normal"
       android:text="no"
       android:layout_marginLeft="10dp"
       android:layout_below="@+id/_contactname"
     	android:layout_toRightOf="@+id/_img"
       />

</RelativeLayout>
</RelativeLayout>
