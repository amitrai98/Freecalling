         <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
     android:background="@drawable/splash"
    android:layout_height="fill_parent"
     >
     <LinearLayout android:id="@+id/_tolayer"
         android:layout_height="wrap_content"
         android:layout_alignParentTop="true"
         android:layout_width="match_parent"
         android:background="@drawable/splash"
         android:orientation="horizontal">
         
         
         
     <Button android:id="@+id/_addnew"
	    android:layout_height="wrap_content"
	    android:layout_width="0dp"
	    android:layout_weight="0.25"
	   	android:layout_gravity="left"
	    android:text="+"/>
	<AutoCompleteTextView android:id="@+id/_findcontact"
	    android:layout_height="wrap_content"
	    android:layout_width="wrap_content"
	    android:layout_marginTop="2dp"
	   
	    android:layout_gravity="right"
	    android:layout_weight=".75"
	    android:singleLine="true"
	    android:scrollHorizontally="false"
	    android:maxLength="10"
	    android:hint="find contact"
	    />
     </LinearLayout>
	
   <ListView android:id="@+id/_contactlist"
       android:layout_height="wrap_content"
       android:background="@android:color/transparent"
       android:cacheColorHint="#0000"
       android:clickable="true"
       android:layout_below="@+id/_tolayer"
       android:layout_width="fill_parent">
       
   </ListView>

</RelativeLayout>
